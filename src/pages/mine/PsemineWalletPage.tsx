import { AlertTriangle, CheckCircle2, Link2, Unplug, WalletCards } from 'lucide-react';
import { PsemineLayout } from '../../components/mine/PsemineLayout';
import { BSC_CHAIN_ID, usePsemineWallet } from '../../contexts/PsemineWalletContext';

export default function PsemineWalletPage() {
  const { address, chainId, isConnecting, isConnected, isBscNetwork, connectWallet, disconnectWallet, switchToBscNetwork } = usePsemineWallet();
  return <PsemineLayout><div className="mx-auto max-w-2xl py-8"><div className="psemine-kicker flex items-center gap-2"><WalletCards size={14} /> Web3 capability</div><h1 className="mt-4 text-4xl font-black tracking-tight text-[#f4f7f2]">Connected wallet</h1><p className="mt-4 text-sm leading-7 text-[#9ca8ac]">Wallet connection is optional and never replaces PSEmine authentication. Payments remain unavailable until an order is created and verified by the backend.</p><div className="psemine-panel mt-10 rounded-3xl p-6 sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-[#9ca8ac]">Current provider</p><p className="mt-2 font-mono text-sm text-[#f4f7f2]">{address ? `${address.slice(0, 8)}...${address.slice(-6)}` : 'No wallet connected'}</p></div><div className={`flex items-center gap-2 rounded-full px-3 py-1 text-[10px] font-black uppercase tracking-widest ${isBscNetwork ? 'bg-[#8fc9a7]/10 text-[#8fc9a7]' : 'bg-[#f0aa3e]/10 text-[#f0aa3e]'}`}>{isBscNetwork ? <CheckCircle2 size={13} /> : <AlertTriangle size={13} />} {isBscNetwork ? 'BSC ready' : chainId ? `Chain ${chainId}` : 'Network unknown'}</div></div><div className="mt-8 flex flex-col gap-3 sm:flex-row">{!isConnected ? <button onClick={connectWallet} disabled={isConnecting} className="psemine-button-primary flex items-center justify-center gap-2 rounded-xl px-5 py-3 text-sm font-black disabled:opacity-50"><Link2 size={16} /> {isConnecting ? 'Connecting...' : 'Connect wallet'}</button> : <button onClick={disconnectWallet} className="psemine-button-secondary flex items-center justify-center gap-2 rounded-xl px-5 py-3 text-sm font-bold"><Unplug size={16} /> Disconnect</button>}{isConnected && !isBscNetwork && <button onClick={switchToBscNetwork} className="psemine-button-secondary rounded-xl px-5 py-3 text-sm font-bold">Switch to BSC</button>}</div><div className="mt-8 border-t border-white/10 pt-5 text-xs leading-6 text-[#758287]">Required purchase network: <span className="font-bold text-[#f4f7f2]">BNB Smart Chain</span> · Chain ID {BSC_CHAIN_ID}</div></div></div></PsemineLayout>;
}
