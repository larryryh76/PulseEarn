import React from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { Activity, ArrowRight, Check, Pickaxe, ShieldCheck, UsersRound, WalletCards } from 'lucide-react';
import { usePsemineAuth } from '../../contexts/PsemineAuthContext';
import PsemineLogo from '../../components/mine/PsemineLogo';

const tools = [['Starter', '£3', '£0.10/hr', '5 copies'], ['Growth', '£10', '£0.50/hr', '3 copies'], ['Pro', '£50', '£1.20/hr', '3 copies'], ['Elite', '£200', '£2.50/hr', '2 copies']];
const previewStats = [['Mining status', 'Active', 'success'], ['Mining rate', '£0.60/hr', 'primary'], ['Owned tools', '3 / 5', 'default'], ['Campaign', 'Genesis', 'default']];
export const PsemineHome: React.FC = () => { const { currentUser, psemineProfile } = usePsemineAuth(); const navigate = useNavigate(); const enter = () => { if (!currentUser) return navigate('/mine/signup'); if (!currentUser.emailVerified) return navigate('/mine/verify-email'); navigate(psemineProfile?.hasCompletedGuide ? '/mine/dashboard' : '/mine/guide'); };
return <div className="psemine-surface min-h-screen overflow-hidden font-sans"><div className="psemine-grid pointer-events-none fixed inset-0 opacity-80" /><header className="relative z-10 border-b border-border"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-8"><Link to="/mine"><PsemineLogo size="sm" /></Link><div className="flex items-center gap-2">{!currentUser && <Link to="/mine/login" className="psemine-button-secondary rounded-xl px-4 py-2.5 text-xs font-bold">Sign in</Link>}<button onClick={enter} className="psemine-button-primary flex items-center gap-2 rounded-xl px-4 py-2.5 text-xs font-black">{currentUser ? 'Open app' : 'Create account'} <ArrowRight size={15} /></button></div></div></header><main className="relative z-10 mx-auto max-w-7xl px-4 pb-20 pt-8 sm:px-8 sm:pt-12"><div className="mb-5 flex items-center gap-2 text-xs font-bold text-text-tertiary"><span className="size-2 rounded-full bg-success" /> Part of the PulseEarn ecosystem <span className="text-border-bright">/</span> Genesis campaign</div><section className="grid items-start gap-5 lg:grid-cols-[.9fr_1.1fr]"><div className="pt-2"><p className="psemine-kicker">PSEmine</p><h1 className="mt-3 max-w-xl text-4xl font-extrabold leading-tight tracking-tight sm:text-6xl">Own tools. Track output. Stay in control.</h1><p className="mt-5 max-w-lg text-base leading-7 text-text-secondary">A focused mining workspace from PulseEarn for tool ownership, verified campaign output, referrals, and wallet activity.</p><div className="mt-7 flex flex-col gap-2 sm:flex-row"><button onClick={enter} className="psemine-button-primary flex items-center justify-center gap-2 rounded-xl px-5 py-3.5 text-sm font-black">Enter Genesis <ArrowRight size={16} /></button><Link to="/mine/guide" className="psemine-button-secondary flex items-center justify-center rounded-xl px-5 py-3.5 text-sm font-bold">How it works</Link></div><div className="mt-7 flex flex-wrap gap-x-5 gap-y-2 text-xs font-semibold text-text-tertiary"><span className="flex items-center gap-1.5"><ShieldCheck size={14} className="text-success" /> Backend verified</span><span className="flex items-center gap-1.5"><WalletCards size={14} className="text-primary" /> BNB Smart Chain</span></div></div><div className="psemine-panel overflow-hidden rounded-2xl"><div className="flex items-center justify-between border-b border-border px-4 py-3"><div><p className="text-xs font-bold text-text-primary">PSEmine workspace</p><p className="text-[10px] text-text-tertiary">Genesis campaign</p></div><span className="psemine-status rounded-full px-2.5 py-1 text-[9px] font-black uppercase tracking-wider">Live state</span></div><div className="grid grid-cols-2 gap-px bg-border">{previewStats.map(([label, value, tone]) => <div key={label} className="bg-surface p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-text-tertiary">{label}</p><p className={`mt-2 text-lg font-extrabold ${tone === 'success' ? 'text-success' : tone === 'primary' ? 'text-primary-bright' : 'text-text-primary'}`}>{value}</p></div>)}</div><div className="border-t border-border p-4"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-wider text-text-tertiary">Current output</p><p className="mt-1 font-mono text-2xl font-bold text-text-primary">£0.0042</p></div><div className="flex items-end gap-1">{[28,42,36,58,48,72,62,84].map((height, i) => <span key={i} className="w-2 rounded-t bg-primary/70" style={{ height }} />)}</div></div></div></div></section><section className="mt-12"><div className="flex items-end justify-between border-b border-border pb-4"><div><p className="psemine-kicker">Genesis tools</p><h2 className="mt-1 text-2xl font-bold">Choose your mining lane</h2></div><p className="hidden max-w-sm text-right text-xs leading-5 text-text-tertiary sm:block">Catalog, rates, and limits are controlled by the campaign configuration.</p></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{tools.map(([tier, price, rate, limit]) => <div key={tier} className="psemine-panel rounded-2xl p-4"><div className="flex items-center justify-between"><span className="text-xs font-bold text-text-primary">{tier}</span><Check size={15} className="text-success" /></div><p className="mt-5 text-2xl font-extrabold">{price}</p><p className="mt-1 text-xs font-bold text-primary-bright">{rate}</p><p className="mt-4 border-t border-border pt-3 text-[10px] text-text-tertiary">Ownership limit: {limit}</p></div>)}</div></section><section className="mt-12 grid gap-3 sm:grid-cols-3">{[[Pickaxe, 'Tool ownership', 'Buy a configured tool and follow its verified mining state.'], [Activity, 'Output and activity', 'See confirmed events and server-provided session values.'], [UsersRound, 'Referrals', 'Track qualified referrals and campaign bonus progress.']].map(([Icon, title, body]) => <div key={title as string} className="psemine-panel rounded-2xl p-4"><Icon size={18} className="text-primary" /><h3 className="mt-4 text-sm font-bold">{title as string}</h3><p className="mt-1 text-xs leading-5 text-text-tertiary">{body as string}</p></div>)}</section><section className="mt-12 flex flex-col items-start justify-between gap-4 rounded-2xl border border-primary/20 bg-primary/5 p-5 sm:flex-row sm:items-center"><div><p className="text-sm font-bold">Ready to see your mining workspace?</p><p className="mt-1 text-xs text-text-secondary">Create an account, verify your email, and complete the PSEmine orientation.</p></div><button onClick={enter} className="psemine-button-primary flex shrink-0 items-center gap-2 rounded-xl px-4 py-3 text-xs font-black">Get started <ArrowRight size={15} /></button></section></main><footer className="relative z-10 border-t border-border px-4 py-6 text-center text-xs text-text-tertiary sm:px-8"><div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-3 sm:flex-row"><PsemineLogo size="sm" /><span>© {new Date().getFullYear()} PulseEarn · PSEmine</span></div></footer></div>; };
export default PsemineHome;
