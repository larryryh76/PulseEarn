[project]
name = "psemine-payment-service"
version = "0.1.0"
description = "Backend-authoritative PSEmine quote and BSC payment verification service"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "httpx>=0.27,<1",
  "pydantic>=2.8,<3",
  "web3>=7,<8",
  "firebase-admin>=6.5,<7",
]
